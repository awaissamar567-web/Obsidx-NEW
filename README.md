# Obsidx-NEW

Obsidx website built with Next.js.

## Run locally

```bash
npm install
npm run dev
```

Copy `.env.example` to `.env.local` and set `OBSIDX_APPLICATION_WEBHOOK_URL` to receive application submissions. Keep `.env.local` private; it is not committed to Git.
